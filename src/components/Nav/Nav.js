import React from 'react';
import './Nav.scss';

const Nav = () => {
    // need to conditionally render buttons based on route
    return (
        <nav className="nav">
            <button>Login</button>
        </nav>
    )
}

export default Nav;